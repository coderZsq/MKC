apiVersion: v1
kind: Service
metadata:
  name: mkc-gateway
  labels:
    app: mkc-gateway
spec:
  selector:
    app: mkc-gateway
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  type: ClusterIP
